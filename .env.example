# Copy this file to .env and fill in what you need. .env is gitignored.

# Only needed for score.py --backend anthropic (the default backend). Get a key at
# https://console.anthropic.com. Not needed for --backend claude-cli, which uses your
# local Claude Code CLI login/subscription instead - see README "Quick start".
ANTHROPIC_API_KEY=
